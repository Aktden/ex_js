Ошибки:
1. 23 строка, клик меняем на submit, чтобы результат отправлялся без перезагрузки
2. 25 строка, добавляем .trim() чтобы убрать пробелы в имени 
3. 42 строка, splice(i) меняем на splice(i, 1) - чтобы при удаление, удалялся только пользователь 
4. 58 строка, добавляем countSpan.textContent = players.length -чтобы счётчик участников обновлялся
5. 74 строка, добавляем обратные кавычки и String() - чтобы время отображалось корректно, так как числаа не имееют метода padStart
6. 80 строка, добавляем проверку if (seconds > 0) - чтобы таймер не уходил в минус
7. 108 строка, добавляем parseInt() - чтобы размер команды был числом, а не строкой
8.  110строка, добавляем проверку if (size <= 0) - чтобы нельзя было создать команду  из отрицательного количества человек
9. 115 строка, players.length < size меняем на players.length === 0 - чтобы можно было создать одну команду из всех участников
